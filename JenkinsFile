pipeline {
    agent any

    tools {
        maven 'Maven'
    }

    environment {
        BROWSER = 'chrome'
        HEADLESS = 'true'
        CUCUMBER_PUBLISH_ENABLED = 'true'
        CUCUMBER_PUBLISH_QUIET = 'false' // set to 'true' if you want minimal logs
    }

    stages {
        stage('Checkout') {
            steps {
                echo 'Cloning repository...'
                git credentialsId: 'bitbucket-creds', url: 'https://bitbucket.org/maseeley/test-automation_kingsley.git', branch: 'extendReportIntegration'
            }
        }

        stage('Run Tests and Auto-Publish Cucumber Report') {
            steps {
                echo "Running tests using browser: ${BROWSER}, headless: ${HEADLESS}"
                sh "mvn verify -Dbrowser=${BROWSER} -Dheadless=${HEADLESS}"
            }
        }
    }

}
